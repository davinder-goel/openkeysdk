<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.openkey.sdk">

    <!-- required permissions for BLE -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!--  <activity android:name=".kaba.activity.MainActivity">
              <intent-filter>
                  <action android:name="android.intent.action.MAIN"/>
                  <category android:name="android.intent.category.LAUNCHER"/>
              </intent-filter>
              <intent-filter>
                  <action android:name="GCM_MESSAGE_RECEIVED"/>
                  <category android:name="android.intent.category.DEFAULT"/>
              </intent-filter>
          </activity>-->

        <!-- NFC service, disable if not needed -->
        <service
            android:name="com.assaabloy.mobilekeys.api.hce.HceService"
            android:enabled="false"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/seos_hce_service" />
        </service>

        <!-- Bluetooth service, disable if not needed -->
        <service android:name="com.assaabloy.mobilekeys.api.ble.BleScanService" />

        <!-- Disable Mixpanel features -->
        <meta-data
            android:name="com.mixpanel.android.MPConfig.DisableDecideChecker"
            android:value="true" />
        <meta-data
            android:name="com.mixpanel.android.MPConfig.DisableViewCrawler"
            android:value="true" />

        <service
            android:name="com.saltosystems.justinkey.sdk.ble.BluetoothLeService"
            android:enabled="true" />


        <!--KABA-->
        <activity android:name=".kaba.Kaba" />
        <activity android:name=".kaba.activity.RegistrationStep1" />
        <activity android:name=".kaba.activity.RegistrationStep2" />

        <!-- GCM support (token management, receiving push messages) -->
        <service
            android:name=".kaba.service.GcmIdListenerService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.android.gms.iid.InstanceID" />
            </intent-filter>
        </service>
        <service
            android:name=".kaba.service.GcmRegistrationIntentService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".kaba.service.GcmMessageReceiver"
            android:exported="true"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <category android:name="com.legic.mobile.app.sdk_quickstart" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
